export enum ROLE {
  client = 'client',
  vendor = 'vendor'
}
export enum LayoutType {
  Banner = 'Banner',
  Faq = 'Faq',
  Category = 'Category'
}
export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}
export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken,
  ActivationCode
}

export enum MediaType {
  Image,
  Video
}
